## To run the app
After cloning the repo create a .env file in which define 2 variables DATABASE_URL and GROQ_API_KEY
1. Run the server
   - cd server
   - npm i
   - npm run dev
2. Run the client
   - cd client
   - npm i
   - npm run dev
   
